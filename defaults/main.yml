virtualbox:
  version: 5.1
  apt_key_url: https://www.virtualbox.org/download/oracle_vbox_2016.asc
  apt_key_url_old: https://www.virtualbox.org/download/oracle_vbox.asc
  apt_repo_url: http://download.virtualbox.org/virtualbox/debian
  users: []

vagrant:
  version: 1.9.8
  deb_url: "https://releases.hashicorp.com/vagrant/{{ vagrant.version }}/vagrant_{{ vagrant.version }}_x86_64.deb"
  deb_sha_url: "https://releases.hashicorp.com/vagrant/{{ vagrant.version }}}/vagrant_{{ vagrant.version }}_SHA256SUMS"
